#include<bits/stdc++.h>
using namespace std;
int main()
{
    char str[1001];
    char str2[1001];
    int len=strlen(gets(str));
    int first_word=0;
    int last_word=1;

    for(int i=0 ; i < len ; i++)
    {
        if(str[i]>A && str[i]<Z )
        {
            str2[i]=str[i];
        }
    }


}
