#include<bits/stdc++.h>
using namespace std;


void find_strng_length(char str[])
{
     int length;
     length=strlen(str);
     cout<<"String Length is :"<<length<<"\n";

}


int main()
{
    char n[100],i;
    int length;
    scanf("%s",&n[i]);
    find_strng_length(n);
    return 0;
}

