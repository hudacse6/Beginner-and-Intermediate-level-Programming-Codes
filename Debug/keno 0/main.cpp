#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
char s[101];
int len;
    cin >> t;
    gets(s);
    while ( t-- )
    {
        gets(s);
        len = strlen ( s );
        cout << s << len<<endl ;
    }

    return 0;
}
