#include<bits/stdc++.h>
using namespace std;
int main()
{
    int Tcase, n;
    int hard, cakewalk;
    int Number_of_problem, participant;
    int a, b, c;
    cin >> Tcase;
    while ( Tcase-- )
    {
        cin >> Number_of_problem >> participant;
        for ( int i = 0 ; i < Number_of_problem ; i++ )
        {
            scanf ( "%d", &n );
            for ( int j = 0 ; j < n ; j++ )
            {

            }
        }
    }
    return 0;
}
