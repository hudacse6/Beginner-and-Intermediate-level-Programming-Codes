#include<bits/stdc++.h>
using namespace std;
int main()

{
    int ar[101]  ;
    int n,k,cont=0,p;
    cin>>n>>k;
    for( int i=1 ; i<= n ; i++)
    {
        cin>>ar[i];
        if( ar[i]>=ar[k] && ar[i]>0)
            cont++
    }

    for( int i=1 ; i<= n ; i++)
    {
        if( ar[i]>=ar[k] && ar[i]>0)
            cont++;
    }
    cout<<cont;
}
