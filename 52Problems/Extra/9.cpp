#include<stdio.h>
#include<string.h>
int main ()
{
    int n, i, tcase;
    scanf ( "%d", &tcase );
    while(tcase--)
    {
        scanf("%d",&n);
        for( i = 0 ; i < n ; i++)
        {

        }

    }
    return 0;
}

